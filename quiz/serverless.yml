service: quiz-api

frameworkVersion: "4"

provider:
  name: aws
  runtime: nodejs20.x
  region: ap-northeast-2
  environment:
    DYNAMODB_TABLE: ${self:service}
    REGION: ${aws:region}
  httpApi:
    cors: true
  iam:
    role:
      statements:
        - Effect: Allow
          Action:
            - dynamodb:Query
            - dynamodb:Scan
            - dynamodb:GetItem
            - dynamodb:PutItem
            - dynamodb:UpdateItem
            - dynamodb:DeleteItem
          Resource:
            - "arn:aws:dynamodb:${aws:region}:*:table/${self:provider.environment.DYNAMODB_TABLE}"
            - "arn:aws:dynamodb:${aws:region}:*:table/${self:provider.environment.DYNAMODB_TABLE}/index/courseId-index"
        - Effect: Allow
          Action:
            - logs:CreateLogGroup
            - logs:CreateLogStream
            - logs:PutLogEvents
          Resource: "*"

functions:
  createQuiz:
    handler: quiz.createQuiz
    events:
      - httpApi:
          path: /v1/create
          method: POST

  getQuiz:
    handler: quiz.getQuiz
    events:
      - httpApi:
          path: /v1/{courseId}/{id}
          method: GET

  listQuizByCourse:
    handler: quiz.listQuizByCourse
    events:
      - httpApi:
          path: /v1/{courseId}
          method: GET

  updateQuiz:
    handler: quiz.updateQuiz
    events:
      - httpApi:
          path: /v1/{courseId}/{id}
          method: PUT

  deleteQuiz:
    handler: quiz.deleteQuiz
    events:
      - httpApi:
          path: /v1/{courseId}/{id}
          method: DELETE

  answerQuiz:
    handler: quiz.answerQuiz
    events:
      - httpApi:
          path: /v1/{courseId}/{id}/answer
          method: POST

resources:
  Resources:
    QuizTable:
      Type: AWS::DynamoDB::Table
      Properties:
        TableName: ${self:provider.environment.DYNAMODB_TABLE}
        BillingMode: PAY_PER_REQUEST
        AttributeDefinitions:
          - AttributeName: id
            AttributeType: S
          - AttributeName: courseId
            AttributeType: S
        KeySchema:
          - AttributeName: id
            KeyType: HASH
        GlobalSecondaryIndexes:
          - IndexName: courseId-index
            KeySchema:
              - AttributeName: courseId
                KeyType: HASH
            Projection:
              ProjectionType: ALL

package:
  patterns:
    - "!node_modules/.bin/**"
    - "node_modules/**"
    - "quiz.mjs"
  individually: false

custom:
  esbuild:
    bundle: true
    minify: false
    format: esm
    sourcemap: true
    target: node20
    platform: node
    outExtension:
      ".js": ".mjs"
    mainFields:
      - module
      - main
